lb:
  image: dockercloud/haproxy
  autorestart: always
  links:
    - web
  ports:
    - "80:3000"
  roles:
    - global
web:
  image: mycaule/ama-bot
  autorestart: always
  deployment_strategy: high_availability
  target_num_containers: 4
